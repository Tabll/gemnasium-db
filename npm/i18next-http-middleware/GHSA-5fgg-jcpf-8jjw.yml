---
identifier: "GHSA-5fgg-jcpf-8jjw"
identifiers:
- "GHSA-5fgg-jcpf-8jjw"
package_slug: "npm/i18next-http-middleware"
title: "i18next-http-middleware: Prototype pollution and path traversal via user-controlled
  language and namespace parameters"
description: "Versions of `i18next-http-middleware` prior to 3.9.3 pass user-controlled
  `lng` and `ns` parameters to two internal paths that use them in ways that enable
  prototype pollution and, depending on the configured backend, path traversal or
  SSRF.\n\nThe vulnerable entry points are unauthenticated HTTP handlers that are
  part of the middleware's public API:\n\n- `getResourcesHandler` — reads `lng`/`ns`
  from query parameters or route params and passes them unvalidated to:\n- `utils.setPath(resources,
  [lng, ns], ...)` — the `setPath` helper did not guard against `__proto__`, `constructor`,
  or `prototype` keys, writing into `Object.prototype` when those values were supplied.\n-
  `i18next.services.backendConnector.load(languages, namespaces, ...)` — depending
  on the configured backend, unvalidated path segments enabled filesystem path traversal
  (e.g. with `i18next-fs-backend`) or SSRF (e.g. with `i18next-http-backend`).\n-
  A `namespaces.forEach(ns => i18next.options.ns.push(ns))` loop additionally performed
  permanent, unbounded growth of the shared singleton namespace list.\n- `missingKeyHandler`
  — iterated the incoming request body with `for...in`, which traverses inherited
  prototype-chain properties. A POST body like `{\"__proto__\": {\"isAdmin\": true}}`
  was forwarded into `saveMissing`."
date: "2026-04-22"
pubdate: "2026-04-22"
affected_range: "<3.9.3"
fixed_versions:
- "3.9.3"
affected_versions: "All versions before 3.9.3"
not_impacted: "All versions starting from 3.9.3"
solution: "Upgrade to version 3.9.3 or above."
urls:
- "https://github.com/advisories/GHSA-5fgg-jcpf-8jjw"
- "https://github.com/i18next/i18next-http-middleware/security/advisories/GHSA-5fgg-jcpf-8jjw"
- "https://github.com/i18next/i18next-http-middleware"
- "https://www.i18next.com/how-to/faq#how-should-the-language-codes-be-formatted"
cvss_v3: "CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:H/A:L"
uuid: "f87e9b71-3afe-4270-9f8c-0869256a23c8"
cwe_ids:
- "CWE-1321"
- "CWE-22"
- "CWE-937"
- "CWE-1035"
