---
identifier: "GHSA-x57h-xx53-v53w"
identifiers:
- "GHSA-x57h-xx53-v53w"
package_slug: "cargo/stellar-xdr"
title: "stellar-xdr's StringM::from_str bypasses max length validation"
description: "`StringM::from_str` does not validate that the input length is within
  the declared maximum (`MAX`). Calling `StringM::<N>::from_str(s)` where `s` is longer
  than `N` bytes succeeds and returns an `Ok` value instead of `Err(Error::LengthExceedsMax)`,
  producing a `StringM` that violates its length invariant.\n\nThis affects any code
  that constructs `StringM` values from string input using `FromStr` (including `str::parse`),
  and relies on the type's maximum length constraint being enforced. An oversized
  `StringM` could propagate through serialization, validation, or other logic that
  assumes the invariant holds.\n\nAll published versions of the `stellar-xdr` crate
  up to and including `v25.0.0` are affected."
date: "2026-03-05"
pubdate: "2026-03-05"
affected_range: "<25.0.1"
fixed_versions:
- "25.0.1"
affected_versions: "All versions before 25.0.1"
not_impacted: "All versions starting from 25.0.1"
solution: "Upgrade to version 25.0.1 or above."
urls:
- "https://github.com/advisories/GHSA-x57h-xx53-v53w"
- "https://github.com/stellar/rs-stellar-xdr/security/advisories/GHSA-x57h-xx53-v53w"
- "https://github.com/stellar/rs-stellar-xdr/issues/499"
- "https://github.com/stellar/rs-stellar-xdr/pull/500"
- "https://github.com/stellar/rs-stellar-xdr/commit/1f840013c3e2fca0321fb844b048afa01d10dda6"
- "https://github.com/stellar/rs-stellar-xdr"
cvss_v3: "CVSS:3.1/AV:L/AC:L/PR:N/UI:N/S:U/C:N/I:L/A:N"
uuid: "0566c9ed-90b3-47d6-af8e-18cb9d9bf0d4"
cwe_ids:
- "CWE-770"
- "CWE-937"
- "CWE-1035"
