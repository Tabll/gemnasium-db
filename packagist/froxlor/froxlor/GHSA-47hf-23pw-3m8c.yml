---
identifier: "GHSA-47hf-23pw-3m8c"
identifiers:
- "GHSA-47hf-23pw-3m8c"
package_slug: "packagist/froxlor/froxlor"
title: "Froxlor has a BIND Zone File Injection via Unsanitized DNS Record Content
  in DomainZones::add()"
description: "`DomainZones::add()` accepts arbitrary DNS record types without a `whitelist`
  and does not sanitize newline characters in the `content` field. When a DNS type
  not covered by the if/elseif validation chain is submitted (e.g., `NAPTR`, `PTR`,
  `HINFO`), content validation is entirely bypassed. Embedded newline characters in
  the content survive `trim()` processing, are stored in the database, and are written
  directly into BIND zone files via `DnsEntry::__toString()`. An authenticated customer
  can inject arbitrary DNS records and BIND directives (`$INCLUDE`, `$ORIGIN`, `$GENERATE`)
  into their domain's zone file."
date: "2026-04-16"
pubdate: "2026-04-16"
affected_range: "<2.3.6"
fixed_versions:
- "2.3.6"
affected_versions: "All versions before 2.3.6"
not_impacted: "All versions starting from 2.3.6"
solution: "Upgrade to version 2.3.6 or above."
urls:
- "https://github.com/advisories/GHSA-47hf-23pw-3m8c"
- "https://github.com/froxlor/froxlor/security/advisories/GHSA-47hf-23pw-3m8c"
- "https://github.com/froxlor/froxlor"
cvss_v3: "CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:C/C:N/I:H/A:L"
uuid: "eb9f9842-497d-483c-9b38-e6925d320e53"
cwe_ids:
- "CWE-93"
- "CWE-937"
- "CWE-1035"
